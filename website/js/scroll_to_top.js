//$("a").get(4).click();  // Simulate a click on the second link in the page.
console.log("Number of links in the page: " + $("a").length);
function scroll_to_top() {
    console.log("Number of links in the page: " + $("a").length);
    $("#sequentially-programmed-web-app-demo").get(0).click();
}
